# ADR Format

The canonical shared format is [grill-with-docs/ADR-FORMAT.md](../grill-with-docs/ADR-FORMAT.md). Read it only when the architecture decision is hard to reverse, surprising without context, and the result of a real trade-off.
