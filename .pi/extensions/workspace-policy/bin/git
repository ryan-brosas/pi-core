#!/usr/bin/env bash
set -euo pipefail
script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
exec "${PI_WORKSPACE_POLICY_NODE:-node}" --experimental-strip-types "$script_dir/../git-wrapper.ts" "$@"
